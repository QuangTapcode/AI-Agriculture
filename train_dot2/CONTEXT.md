# YOLO training context

This area contains the agricultural quality-detection training workflow. Keep datasets, weights, evaluation reports, and model deployment paths explicit. Frontend confidence values must come from inference responses and must never be manufactured for presentation.
